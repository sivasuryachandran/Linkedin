# Secret values — base64-encoded.
# IMPORTANT: Replace these placeholder values before deploying to a real cluster.
# In production, use AWS Secrets Manager with the CSI driver or ExternalSecrets
# operator instead of storing credentials in manifest files.
#
# To encode a value: echo -n 'your-password' | base64
apiVersion: v1
kind: Secret
metadata:
  name: linkedin-secrets
  namespace: linkedin-platform
type: Opaque
data:
  # echo -n 'rootpassword' | base64
  MYSQL_ROOT_PASSWORD: cm9vdHBhc3N3b3Jk
  # echo -n 'linkedin_user' | base64
  MYSQL_USER: bGlua2VkaW5fdXNlcg==
  # echo -n 'linkedin_pass' | base64
  MYSQL_PASSWORD: bGlua2VkaW5fcGFzcw==
  # echo -n 'mongo_user' | base64
  MONGO_USER: bW9uZ29fdXNlcg==
  # echo -n 'mongo_pass' | base64
  MONGO_PASSWORD: bW9uZ29fcGFzcw==
